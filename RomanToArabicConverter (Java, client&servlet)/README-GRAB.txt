Z nieznanych przyczyn skrypt generowany przez plik .grab nie odpowiada w³aœciwoœciom tabeli.
Problem dotyczy automatycznego inkrementowania kolumny ID poprzez w³asnoœæ GENERATED BY DEFAULT AS IDENTITY.
W wyniku tego niemo¿liwe jest prawid³owe wygenerowanie tabeli. Po wyœwietleniu b³êdu skrypt nale¿y poprawiæ
na poni¿szy:

create table RESULTS
(
	ID BIGINT GENERATED BY DEFAULT AS IDENTITY primary key,
	ARABICNUMERAL SMALLINT not null,
	ROMANNUMERAL CHAR(15) not null,
	DATE TIMESTAMP not null,
	ADDRESS CHAR(15) not null
)

